// Practice file for testing LSP features
// Try: autocomplete, go-to-definition, hover documentation

function calculateTotal(items: number[]): number {
  return items.reduce((sum, item) => sum + item, 0);
}

// Test calling the function - autocomplete should suggest it
const result =

